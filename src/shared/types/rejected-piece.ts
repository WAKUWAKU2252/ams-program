// ชนิดข้อมูลที่ RequestTable กับ DraftForm ใช้ร่วมกัน
// แยกออกมาไว้นอก .vue เพราะ <script setup> ประกาศ export ไม่ได้ (เหมือน asset-form.ts)

/**
 * ชิ้นที่บัญชีตีกลับและยังรอผู้ขอแก้ - RequestTable รู้เรื่องนี้เพราะมันโหลด slots อยู่แล้ว
 * แล้วส่งขึ้นไปให้ DraftForm ขึ้น banner ว่า "ต้องแก้ชิ้นไหนบ้าง"
 *
 * ★ มีแค่เลขชิ้นโดยตั้งใจ - ไม่เอาเหตุผล/ชื่อของ/เลขรอบมาด้วย เพราะทั้งสามอย่างนั้นอยู่ที่
 *   แถวของชิ้นนั้นในตารางข้างล่างแล้ว (ปุ่มโน้ต) และในกล่องกรอกตอนกดเข้าไปแก้
 *   ตีกลับ 5 ชิ้น = banner 6 บรรทัด ซึ่งดันตารางลงไปพ้นจอโดยไม่ได้บอกอะไรใหม่
 *   หน้าที่ของ banner คือ "ไปดูตรงไหน" ไม่ใช่เอาเนื้อหาของที่นั่นมาแสดงซ้ำ
 *
 * ★ ทำไมต้องส่งขึ้นไป ไม่ให้ DraftForm ยิง getAssetSlots เอง: จะได้สองสายที่ถามคำถาม
 *   เดียวกันแล้วตอบไม่ตรงกันได้ (คนละจังหวะโหลด) - banner บอก 2 ชิ้นแต่ตารางโชว์ 3 ชิ้น
 *   อ่านไม่ออกเลยว่าอันไหนจริง แบบนี้ตารางเป็นแหล่งเดียวตามเดิม (ทางเดียวกับ @over-cost)
 */
export interface RejectedPiece {
  /** เลขที่ผู้ใช้เห็นบนตาราง - poLine.unitNo เช่น "1.3" ต้องตรงกับที่ตารางโชว์เป๊ะ */
  poLine: number;
  unitNo: number;
}
